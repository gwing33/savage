# Secrets — never commit
settings.toml

# CircuitPython libraries installed by circup — device-specific binaries,
# not source code. Install on each Pico with:
#   circup install adafruit_sen6x adafruit_minimqtt adafruit_connection_manager
lib/

# CircuitPython runtime artifacts (if you mount CIRCUITPY here)
.fseventsd/
.metadata_never_index
.Trashes
boot_out.txt
.DS_Store

# Editor noise
.vscode/
.idea/
*.swp
